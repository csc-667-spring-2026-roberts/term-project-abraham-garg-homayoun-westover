<%- include("partials/header", { title, currentUserId }) %>

<h1>Spades</h1>
<p class="muted-text">Welcome, <%= user.username %>! Create or join a game below.</p>

<ul class="info-list">
  <li>👤 <strong><%= user.id %></strong></li>
  <li>✉️ <%= user.email %></li>
  <li>📅 <%= user.created_at.toISOString().split("T")[0] %></li>
</ul>

<h2>Games</h2>

<button id="loadBtn">Create New Game</button>

<template id="itemTemplate">
  <div class="item">
    <p class="name"></p>
  </div>
</template>

<div id="container"></div>

<%- include("partials/footer") %>
